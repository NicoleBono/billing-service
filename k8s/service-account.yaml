apiVersion: v1
kind: ServiceAccount
metadata:
  name: billing-service
  namespace: oficina
  annotations:
    # Preencher com o output saga_service_role_arns.billing-service do
    # `terraform apply` em infra-kubernetes (IRSA).
    eks.amazonaws.com/role-arn: ""
